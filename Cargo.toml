[workspace]
members = ["openemc-log", "openemc-pack", "xtask"]
exclude = [
    "defmt-ringbuf",
    "openemc-bootloader",
    "openemc-firmware",
    "openemc-shared",
]

[profile.release]
opt-level = 'z'
lto = 'fat'
strip = 'symbols'
